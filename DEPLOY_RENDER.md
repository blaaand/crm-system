# 🚀 دليل رفع المشروع على Render (الخطة المجانية)

> 💡 **ملاحظة مهمة**: Render يتوقف بعد 15 دقيقة وقاعدة البيانات مجانية لمدة 90 يوم فقط.
> 
> ✅ **للاستضافة المجانية الدائمة**: انظر [FREE_HOSTING_OPTIONS.md](./FREE_HOSTING_OPTIONS.md) للحصول على خيارات مجانية دائمة مثل Supabase + Vercel + Railway

## 📋 المتطلبات الأساسية

1. حساب مجاني على [render.com](https://render.com) (سجل مجاناً)
2. Git repository (GitHub/GitLab/Bitbucket) - يمكنك إنشاء واحد مجاناً

---

## ⚠️ ملاحظات مهمة قبل البدء

### الخطة المجانية على Render:
- ⚠️ **PostgreSQL**: **90 يوم مجاناً فقط** (ثم يتم حذف البيانات)
- ⚠️ **Web Service**: يتوقف بعد **15 دقيقة** من عدم الاستخدام
- ✅ **Static Site**: مجاني بالكامل
- ⚠️ **تحذير**: عند أول طلب بعد التوقف، قد يستغرق **30-60 ثانية** للبدء

### 🆓 خيارات مجانية دائمة:
- 📄 انظر [FREE_HOSTING_OPTIONS.md](./FREE_HOSTING_OPTIONS.md) للحصول على خيارات مثل:
  - **Supabase**: Database مجاني دائماً
  - **Vercel**: Frontend مجاني دائماً  
  - **Railway**: Backend مع $5 رصيد مجاني شهرياً

---

## 📝 الخطوة 1: إنشاء قاعدة البيانات PostgreSQL

### 1.1 إنشاء Database
1. سجل دخول إلى [render.com](https://dashboard.render.com)
2. اضغط على **New +** (أعلى اليسار)
3. اختر **PostgreSQL**

### 1.2 إعدادات Database
املأ البيانات التالية:

| الحقل | القيمة |
|-------|--------|
| **Name** | `crm-database` |
| **Database** | `crm_db` |
| **User** | `crm_user` |
| **Region** | `Frankfurt` (أو أقرب منطقة لك) |
| **PostgreSQL Version** | `16` (أو آخر إصدار) |
| **Plan** | **Free** ✅ |

4. اضغط **Create Database**

### 1.3 نسخ رابط الاتصال
1. بعد الإنشاء (30 ثانية)، اذهب إلى صفحة Database
2. افتح قسم **Connections**
3. انسخ **Internal Database URL** (يبدأ بـ `postgresql://`)
4. **احفظ هذا الرابط** - ستحتاجه في الخطوة التالية

**مثال على الرابط**:
```
postgresql://crm_user:password@dpg-xxxxx-a.frankfurt-postgres.render.com/crm_db
```

---

## 🔧 الخطوة 2: إعداد Backend Service

### 2.1 رفع المشروع إلى Git
1. ارفع المشروع إلى GitHub/GitLab/Bitbucket
2. تأكد من رفع جميع الملفات (باستثناء node_modules و dist)

### 2.2 إنشاء Web Service
1. في Render Dashboard → اضغط **New +**
2. اختر **Web Service**
3. اختر **Connect** بجانب Git repository الخاص بك
4. إذا لم يكن متصل، اضغط **Configure account** أولاً

### 2.3 إعدادات Backend
املأ البيانات التالية:

| الحقل | القيمة |
|-------|--------|
| **Name** | `crm-backend` |
| **Region** | نفس منطقة Database (Frankfurt) |
| **Branch** | `main` أو `master` |
| **Root Directory** | `backend` |
| **Environment** | `Node` |
| **Build Command** | `npm install && npx prisma generate && npm run build` |
| **Start Command** | `npx prisma migrate deploy && npm run start:prod` |

### 2.4 Environment Variables
في قسم **Environment Variables**، اضغط **Add Environment Variable** وأضف:

| Key | Value | ملاحظات |
|-----|-------|---------|
| `NODE_ENV` | `production` | - |
| `DATABASE_URL` | `<الرابط من الخطوة 1.3>` | ⚠️ مهم جداً |
| `JWT_SECRET` | `<أنشئ مفتاح سري>` | انظر الأسفل |
| `CORS_ORIGIN` | `https://crm-frontend.onrender.com` | سيتم تغييره لاحقاً |
| `PORT` | `3000` | - |

#### كيفية إنشاء JWT_SECRET:
افتح Terminal/PowerShell واكتب:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
انسخ الناتج وضعه في `JWT_SECRET`

**إذا كنت تستخدم AWS S3** (اختياري):
| Key | Value |
|-----|-------|
| `AWS_ACCESS_KEY_ID` | مفتاح AWS الخاص بك |
| `AWS_SECRET_ACCESS_KEY` | المفتاح السري |
| `AWS_REGION` | `us-east-1` |
| `AWS_S3_BUCKET` | اسم الـ bucket |

### 2.5 إنشاء Service
1. اضغط **Create Web Service**
2. انتظر حتى يبدأ Build (5-10 دقائق في المرة الأولى)
3. ✅ عندما ترى **Live** في الأعلى، يعني Backend يعمل

---

## 🎨 الخطوة 3: إعداد Frontend (Static Site)

### 3.1 إنشاء Static Site
1. في Render Dashboard → اضغط **New +**
2. اختر **Static Site**
3. اختر Git repository الخاص بك

### 3.2 إعدادات Frontend
املأ البيانات التالية:

| الحقل | القيمة |
|-------|--------|
| **Name** | `crm-frontend` |
| **Region** | نفس منطقة Backend |
| **Branch** | `main` أو `master` |
| **Root Directory** | `frontend` |
| **Build Command** | `npm install && npm run build` |
| **Publish Directory** | `dist` |

### 3.3 Environment Variables
في قسم **Environment Variables**، أضف:

| Key | Value |
|-----|-------|
| `VITE_API_URL` | `https://crm-backend.onrender.com/api` |

⚠️ **مهم**: استبدل `crm-backend` باسم Backend Service الفعلي الذي أنشأته

### 3.4 تحديث CORS في Backend
بعد أن تحصل على رابط Frontend:
1. اذهب إلى Backend Service
2. افتح **Environment** tab
3. عدل `CORS_ORIGIN` إلى رابط Frontend الفعلي:
   ```
   https://crm-frontend.onrender.com
   ```
4. اضغط **Save Changes** (سيتم إعادة التشغيل تلقائياً)

### 3.5 إنشاء Site
1. اضغط **Create Static Site**
2. انتظر حتى يكتمل Build (3-5 دقائق)
3. ✅ عندما ترى **Live**، Frontend جاهز

---

## ✅ الخطوة 4: التحقق من نجاح الرفع

### 4.1 فحص Backend
1. افتح رابط Backend: `https://crm-backend.onrender.com/api/docs`
2. ✅ يجب أن تظهر صفحة Swagger API Documentation
3. إذا ظهرت رسالة خطأ، تحقق من **Logs** في Render Dashboard

### 4.2 فحص Frontend
1. افتح رابط Frontend: `https://crm-frontend.onrender.com`
2. ✅ يجب أن تظهر صفحة تسجيل الدخول
3. إذا لم يظهر شيء أو ظهر خطأ، تحقق من **Logs**

### 4.3 إنشاء مستخدم Admin
بما أنه لا يوجد مستخدم بعد:
1. افتح Backend Swagger: `https://crm-backend.onrender.com/api/docs`
2. ابحث عن endpoint `POST /admin/users`
3. اضغط **Try it out**
4. املأ البيانات:
   ```json
   {
     "name": "Admin",
     "email": "admin@crm.com",
     "phone": "0500000000",
     "password": "admin123",
     "role": "ADMIN"
   }
   ```
5. اضغط **Execute**
6. ✅ يجب أن تحصل على `201 Created`

### 4.4 تسجيل الدخول
1. اذهب إلى Frontend
2. استخدم البيانات:
   - **البريد**: `admin@crm.com`
   - **كلمة المرور**: `admin123`
3. ✅ يجب أن تتمكن من تسجيل الدخول

---

## 🔧 استكشاف الأخطاء وحلها

### ❌ المشكلة: Backend لا يعمل

#### التحقق من Logs:
1. اذهب إلى Backend Service
2. اضغط **Logs** tab
3. ابحث عن الأخطاء الحمراء

#### الأخطاء الشائعة:

**خطأ: `DATABASE_URL` is not defined**
- ✅ تأكد من إضافة `DATABASE_URL` في Environment Variables
- ✅ تأكد من أن الرابط يبدأ بـ `postgresql://`

**خطأ: `relation "users" does not exist`**
- ✅ هذا يعني أن Migrations لم تعمل
- ✅ افتح **Shell** في Backend Service
- ✅ نفذ: `cd backend && npx prisma migrate deploy`

**خطأ: `ECONNREFUSED` أو Database connection failed**
- ✅ تحقق من أن Database يعمل (يجب أن يكون **Available**)
- ✅ تأكد من نسخ **Internal Database URL** وليس External
- ✅ تأكد من أن Database و Backend في نفس Region

**خطأ: Build failed**
- ✅ تحقق من Logs في قسم **Build Logs**
- ✅ تأكد من أن جميع الملفات موجودة في Git repository
- ✅ تأكد من أن `Root Directory` هو `backend` وليس `.`

---

### ❌ المشكلة: Frontend لا يتصل مع Backend

**Frontend يظهر خطأ في Console: `Network Error`**
- ✅ تحقق من `VITE_API_URL` في Frontend Environment Variables
- ✅ تأكد من أن Backend يعمل (افتح رابط Backend)
- ✅ تحقق من `CORS_ORIGIN` في Backend (يجب أن يحتوي على رابط Frontend)

**Frontend يظهر `CORS Error`**
- ✅ تأكد من أن `CORS_ORIGIN` في Backend يحتوي على رابط Frontend بالضبط
- ✅ مثال صحيح: `https://crm-frontend.onrender.com`
- ✅ مثال خاطئ: `https://crm-frontend.onrender.com/` (بخط مائل في النهاية)

**Frontend يعرض صفحة بيضاء**
- ✅ افتح Console في المتصفح (F12)
- ✅ ابحث عن الأخطاء
- ✅ تحقق من Logs في Render Dashboard

---

### ❌ المشكلة: قاعدة البيانات فارغة

**لا توجد جداول في Database**
- ✅ Migrations لم تعمل
- ✅ افتح **Shell** في Backend Service
- ✅ نفذ:
  ```bash
  cd backend
  npx prisma migrate deploy
  ```
- ✅ إذا فشل، جرب:
  ```bash
  npx prisma db push
  ```

**لا يوجد مستخدم Admin**
- ✅ استخدم Swagger UI لإنشاء مستخدم (انظر 4.3)
- ✅ أو استخدم Shell:
  ```bash
  cd backend
  npx prisma studio
  ```
  (ثم أضف مستخدم يدوياً)

---

## 📝 ملاحظات مهمة للخطة المجانية

### ⏰ التوقف التلقائي:
- الخدمات المجانية تتوقف بعد **15 دقيقة** من عدم الاستخدام
- عند أول طلب بعد التوقف، يستغرق **30-60 ثانية** للبدء
- هذا طبيعي ولا يعني وجود خطأ

### 💾 قاعدة البيانات:
- **Free PostgreSQL** متاح لمدة **90 يوم** فقط
- بعد 90 يوم، يتم **حذف البيانات** تلقائياً
- للاحتفاظ بالبيانات، انتقل إلى Paid Plan قبل انتهاء 90 يوم

### 🚀 الأداء:
- الخطة المجانية مناسبة للتجربة والاختبار
- للإنتاج أو الاستخدام الفعلي، استخدم Paid Plan
- Free Plan له حدود في عدد الطلبات والموارد

### 🔒 الأمان:
- ✅ لا تشارك Environment Variables مع أحد
- ✅ استخدم `JWT_SECRET` قوي وفريد
- ✅ قم بتحديث كلمات المرور بانتظام
- ✅ لا ترفع ملفات `.env` إلى Git

---

## ✅ قائمة التحقق النهائية

قبل اعتبار المشروع جاهزاً، تأكد من:

- [ ] Backend يعمل: `https://crm-backend.onrender.com/api/docs`
- [ ] Frontend يعمل: `https://crm-frontend.onrender.com`
- [ ] قاعدة البيانات متصلة (لا توجد أخطاء في Backend Logs)
- [ ] تم إنشاء مستخدم Admin
- [ ] تسجيل الدخول يعمل
- [ ] CORS يعمل (لا توجد أخطاء في Console)
- [ ] جميع Environment Variables محددة بشكل صحيح

---

## 🆘 الحصول على المساعدة

### 📚 الموارد:
- [Render Documentation](https://render.com/docs)
- [Prisma Production Guide](https://www.prisma.io/docs/guides/deployment)
- [NestJS Deployment](https://docs.nestjs.com/faq/deployment)

### 🔍 التحقق من المشاكل:
1. تحقق من **Logs** في Render Dashboard
2. تحقق من **Metrics** لمعرفة استخدام الموارد
3. تحقق من **Events** لمعرفة ما حدث مؤخراً

---

## 🎉 مبروك!

إذا وصلت هنا، يعني أن المشروع يعمل على Render بنجاح! 🎊

**الخطوات التالية**:
- أنشئ مستخدمين جدد
- ابدأ استخدام النظام
- راقب Logs للتأكد من عدم وجود مشاكل
