# 🔧 إضافة DATABASE_URL في Railway

## 🔴 المشكلة:
```
Error: Environment variable not found: DATABASE_URL
```

## ✅ الحل: إضافة DATABASE_URL في Railway

---

## 📍 أين تجد Environment Variables في Railway:

### الطريقة:

1. **افتح Railway Dashboard:**
   - [railway.app](https://railway.app)

2. **اختر المشروع:**
   - Projects → `crm-system`

3. **اختر Backend Service:**
   - اضغط على **Backend Service**

4. **افتح Variables:**
   - في القائمة العلوية، اضغط **Variables** ⚙️

5. **أضف DATABASE_URL:**
   - اضغط **+ New Variable** أو **Add Variable**
   - في حقل **Name**: اكتب `DATABASE_URL`
   - في حقل **Value**: الصق رابط Database

---

## 🔗 من أين تحصل على DATABASE_URL؟

### الخيار 1: Supabase (مجاني دائماً) - موصى به ⭐

#### خطوات الحصول على DATABASE_URL:

1. **اذهب إلى Supabase:**
   - [supabase.com](https://supabase.com)
   - سجّل دخول

2. **اختر Project أو أنشئ واحد جديد:**
   - إذا لم يكن لديك Project، اضغط **New Project**
   - املأ البيانات:
     - **Name**: `crm-database`
     - **Database Password**: (اختر كلمة مرور قوية واحفظها!)
     - **Region**: اختر أقرب منطقة
     - **Plan**: **Free** ✅

3. **انتظر حتى يكتمل الإنشاء** (1-2 دقيقة)

4. **الحصول على Connection String:**
   - اذهب إلى **Project Settings** (⚙️ في القائمة الجانبية)
   - اضغط **Database** من القائمة
   - في قسم **Connection string**، اختر **URI**
   - الصيغة: `postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres`
   
   ⚠️ **مهم:** استبدل `[YOUR-PASSWORD]` بكلمة المرور التي اخترتها!

5. **انسخ الرابط كاملاً**

---

### الخيار 2: Railway PostgreSQL

#### خطوات الحصول على DATABASE_URL:

1. **في Railway Dashboard:**
   - في نفس المشروع (`crm-system`)

2. **أضف PostgreSQL Database:**
   - اضغط **+ New** → **Database** → **Add PostgreSQL**
   - Railway سيقوم بإنشاء Database تلقائياً

3. **الحصول على DATABASE_URL:**
   - اضغط على **PostgreSQL** Service
   - اذهب إلى **Variables** tab
   - ستجد `DATABASE_URL` موجود تلقائياً!
   - انسخه

---

## 📝 إضافة DATABASE_URL في Railway:

### الخطوات:

1. **في Backend Service:**
   - اضغط **Variables** (في القائمة العلوية)

2. **أضف Variable جديد:**
   - اضغط **+ New Variable** أو **Add Variable**

3. **املأ البيانات:**
   ```
   Name: DATABASE_URL
   Value: postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
   ```

4. **احفظ:**
   - اضغط **Save** أو **Add**

---

## ✅ Environment Variables المطلوبة الأخرى:

بينما أنت في **Variables**، تأكد من وجود جميع المتغيرات التالية:

| Name | Value | مثال |
|------|-------|------|
| `NODE_ENV` | `production` | `production` |
| `DATABASE_URL` | `postgresql://...` | من Supabase أو Railway |
| `JWT_SECRET` | `<أنشئ مفتاح>` | `your-secret-key-here` |
| `CORS_ORIGIN` | `<رابط Frontend>` | `https://crm-frontend.vercel.app` |
| `PORT` | `3000` | `3000` |

---

## 🔑 إنشاء JWT_SECRET:

إذا لم يكن لديك `JWT_SECRET`:

1. **في PowerShell محلياً:**
   ```powershell
   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
   ```

2. **انسخ الناتج** واستخدمه كـ `JWT_SECRET`

---

## ✅ بعد إضافة DATABASE_URL:

1. ✅ **حفظ التغييرات** في Railway
2. ✅ **Redeploy** Service:
   - اذهب إلى **Deployments**
   - اضغط **Redeploy**

---

## 🔍 التحقق من النجاح:

بعد Redeploy، في Logs يجب أن ترى:

```
✅ Prisma schema loaded from prisma/schema.prisma
✅ Datasource "db": PostgreSQL database
✅ Applying migrations...
✅ Migrations applied
✅ Starting application...
🚀 Application is running...
```

**بدون** خطأ `Environment variable not found: DATABASE_URL` ✅

---

## 📸 الصفحة في Railway (وصفي):

```
┌─────────────────────────────────────┐
│ Backend Service                     │
├─────────────────────────────────────┤
│ [Deployments] [Metrics] [Settings]  │
│    [Variables] ← اضغط هنا ← ← ← ←  │
│            [Logs]                   │
├─────────────────────────────────────┤
│                                     │
│ Environment Variables              │
│                                     │
│ ┌───────────────────────────────┐ │
│ │ Name           │ Value        │ │
│ ├───────────────────────────────┤ │
│ │ NODE_ENV       │ production   │ │
│ │ DATABASE_URL   │ (فارغ!) ←   │ │ ← أضف هنا!
│ │ JWT_SECRET     │ ...          │ │
│ │ CORS_ORIGIN    │ ...          │ │
│ │ PORT           │ 3000         │ │
│ └───────────────────────────────┘ │
│                                     │
│ [+ New Variable] ← أو اضغط هنا     │
│                                     │
└─────────────────────────────────────┘
```

عند الضغط على **+ New Variable**:
```
┌─────────────────────────────────────┐
│ Add Environment Variable            │
├─────────────────────────────────────┤
│                                     │
│ Name:  [DATABASE_URL        ]       │ ← اكتب هنا
│                                     │
│ Value: [postgresql://postgres...]   │ ← الصق هنا
│                                     │
│ [Cancel]  [Save] ← اضغط Save        │
│                                     │
└─────────────────────────────────────┘
```

---

## 🆘 إذا لم تجد Variables:

1. ✅ تأكد أنك داخل **Backend Service** (وليس Project)
2. ✅ تأكد أن Service موجود
3. ✅ جرب Refresh الصفحة

---

## 💡 نصيحة:

### استخدام Supabase (موصى به):

- ✅ **مجاني دائماً**
- ✅ 500MB Database (كافية للبداية)
- ✅ لا قيود زمنية
- ✅ سريع وسهل

**الخطوات:**
1. سجّل في [supabase.com](https://supabase.com)
2. أنشئ Project
3. انسخ Connection String
4. ضعه في Railway Variables

---

## ✅ بعد إضافة DATABASE_URL:

1. ✅ **Save** في Railway
2. ✅ **Redeploy** Service
3. ✅ تحقق من Logs - يجب أن يعمل الآن! 🚀

أخبرني إذا نجح! 🎉

