# 📤 دليل رفع المشروع على GitHub

## ✅ الخطوات المطلوبة

### 1. إنشاء Repository على GitHub

1. اذهب إلى [github.com](https://github.com)
2. اضغط **+** في أعلى الصفحة → **New repository**
3. املأ البيانات:
   - **Repository name**: `crm-system` (أو أي اسم تريده)
   - **Description**: `نظام إدارة العملاء والطلبات (CRM System)`
   - **Visibility**: اختر **Public** (مجاني) أو **Private** (مقترح)
   - ❌ **لا** تضع علامة على "Initialize this repository with a README"
4. اضغط **Create repository**

### 2. ربط المشروع المحلي بـ GitHub

بعد إنشاء الـ Repository، ستظهر لك تعليمات. اتبع أحد الخيارات:

#### إذا كان المشروع فارغاً (أول مرة):
```bash
git remote add origin https://github.com/YOUR_USERNAME/crm-system.git
git branch -M main
git push -u origin main
```

#### إذا كان المشروع يحتوي على ملفات:
```bash
git remote add origin https://github.com/YOUR_USERNAME/crm-system.git
git branch -M main
git push -u origin main
```

### 3. رفع الملفات

```bash
# إضافة جميع الملفات
git add .

# حفظ التغييرات
git commit -m "Initial commit: CRM System"

# رفع إلى GitHub
git push -u origin main
```

---

## 🔐 استخدام HTTPS (بكلمة مرور)

إذا طُلب منك اسم المستخدم وكلمة المرور:
- **Username**: اسم مستخدم GitHub الخاص بك
- **Password**: **Personal Access Token** (وليس كلمة مرور GitHub العادية)

### إنشاء Personal Access Token:

1. اذهب إلى GitHub → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. اضغط **Generate new token (classic)**
3. املأ:
   - **Note**: `CRM System Deployment`
   - **Expiration**: اختر مدة (مثل 90 يوم)
   - **Select scopes**: ✅ حدد `repo` (Full control of private repositories)
4. اضغط **Generate token**
5. **انسخ الـ Token فوراً** (لن تتمكن من رؤيته مرة أخرى)
6. استخدم هذا الـ Token كـ Password عند الـ push

---

## 🔑 استخدام SSH (موصى به)

إذا كنت تفضل استخدام SSH بدلاً من HTTPS:

### 1. إنشاء SSH Key (إذا لم يكن موجود):

```bash
# إنشاء SSH key
ssh-keygen -t ed25519 -C "your_email@example.com"

# اضغط Enter لكل الأسئلة (أو غير المسار إذا أردت)
```

### 2. إضافة SSH Key إلى GitHub:

```bash
# عرض المفتاح العام
cat ~/.ssh/id_ed25519.pub
# أو في Windows:
type %USERPROFILE%\.ssh\id_ed25519.pub
```

1. انسخ المحتوى كاملاً
2. اذهب إلى GitHub → **Settings** → **SSH and GPG keys**
3. اضغط **New SSH key**
4. الصق المفتاح واضغط **Add SSH key**

### 3. تغيير Remote URL إلى SSH:

```bash
git remote set-url origin git@github.com:YOUR_USERNAME/crm-system.git
```

---

## 📋 الملفات التي سيتم رفعها

✅ **سيتم رفعها:**
- جميع ملفات الكود (`.ts`, `.tsx`, `.js`, `.json`)
- ملفات التكوين (`package.json`, `tsconfig.json`, إلخ)
- ملفات Prisma Schema
- ملفات التوثيق (`.md`)
- ملفات `.bat` للتنفيذ

❌ **لن يتم رفعها** (بسبب `.gitignore`):
- `node_modules/` (التبعيات)
- `.env` (المتغيرات السرية)
- `dist/` (ملفات البناء)
- `dev.db` (قاعدة البيانات المحلية)
- ملفات مؤقتة

---

## ✅ التحقق من النجاح

بعد الرفع، تأكد من:
1. ✅ فتح Repository على GitHub
2. ✅ التحقق من أن جميع الملفات موجودة
3. ✅ التحقق من أن `.env` غير موجود (مهم للخصوصية!)

---

## 🔄 التحديثات المستقبلية

عند إجراء تغييرات:

```bash
# إضافة التغييرات
git add .

# حفظ التغييرات
git commit -m "وصف التغييرات"

# رفع التغييرات
git push
```

---

## ⚠️ ملاحظات مهمة

### 🔐 الأمان:
- ❌ **لا ترفع `.env`** - يحتوي على معلومات سرية
- ❌ **لا ترفع `dev.db`** - قاعدة البيانات المحلية
- ✅ استخدم Environment Variables في منصة الرفع (Render/Railway)

### 📦 الحجم:
- إذا كان المشروع كبيراً جداً، قد تحتاج إلى:
  - حذف `node_modules` قبل الرفع (سيتم تنزيله لاحقاً)
  - استخدام Git LFS للملفات الكبيرة

---

## 🚀 بعد الرفع

بعد رفع المشروع على GitHub، يمكنك:
1. ✅ ربطه بـ **Render** (اتبع `DEPLOY_RENDER.md`)
2. ✅ ربطه بـ **Railway** (اتبع `FREE_HOSTING_OPTIONS.md`)
3. ✅ ربطه بـ **Vercel** (للـ Frontend)

جميع منصات الرفع تدعم GitHub Integration!

---

## 🆘 حل المشاكل

### خطأ: "Repository not found"
- ✅ تحقق من اسم المستخدم والـ Repository
- ✅ تحقق من الصلاحيات (يجب أن يكون Repository خاص بك)

### خطأ: "Permission denied"
- ✅ استخدم Personal Access Token بدلاً من كلمة المرور
- ✅ أو استخدم SSH Keys

### خطأ: "Large file"
- ✅ تأكد من أن `.gitignore` يمنع الملفات الكبيرة
- ✅ استخدم `git rm --cached <file>` لحذف ملف كبير تم رفعه بالخطأ

---

## 📞 المساعدة

إذا واجهت أي مشكلة، أخبرني وسأساعدك!

