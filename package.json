{
  "name": "crm-system",
  "version": "1.0.0",
  "description": "CRM System - Backend and Frontend",
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma migrate deploy && npm run start:prod"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}

