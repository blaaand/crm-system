# 🔍 تشخيص مشكلة VITE_API_URL

## ✅ **الكود صحيح تماماً!**

الكود الحالي في `frontend/src/services/api.ts`:

```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || '/api'

export const api = axios.create({
  baseURL: API_BASE_URL,
  // ...
})
```

**هذا صحيح 100%** ✅

---

## ❌ **المشكلة الحقيقية:**

متغير `VITE_API_URL` **غير معرف** في منصة الاستضافة (Vercel/Railway/Render).

---

## 🔍 **كيف تتحقق من المشكلة؟**

### **الخطوة 1: افتح موقعك في المتصفح**

افتح الموقع المُنشَر (مثل: `https://your-frontend.vercel.app`)

### **الخطوة 2: افتح Developer Console**

- اضغط **F12**
- انتقل إلى **Console**

### **الخطوة 3: تحقق من VITE_API_URL**

في Console، اكتب:
```javascript
console.log('VITE_API_URL:', import.meta.env.VITE_API_URL)
```

**اضغط Enter**

---

## 📊 **النتائج المحتملة:**

### **إذا ظهر `undefined`:**
```
VITE_API_URL: undefined
```

**✅ هذا يعني:** المتغير غير معرف في Environment Variables

**🔧 الحل:** أضف `VITE_API_URL` في منصة الاستضافة

---

### **إذا ظهر `/api`:**
```
VITE_API_URL: /api
```

**❌ هذا يعني:** المتغير موجود لكن القيمة خاطئة (سيكون محلي فقط)

**🔧 الحل:** حدّث `VITE_API_URL` إلى Backend URL الصحيح

---

### **إذا ظهر URL كامل:**
```
VITE_API_URL: https://crm-backend.railway.app/api
```

**✅ هذا يعني:** المتغير معرف بشكل صحيح!

**🔍 إذا استمرت المشكلة:** المشكلة في Backend وليست في Frontend

---

## 🆘 **كيف تضيف VITE_API_URL؟**

### **إذا Frontend على Vercel:**

1. **اذهب إلى:** https://vercel.com/dashboard
2. **افتح Project** → **Settings** → **Environment Variables**
3. **أضف متغير جديد:**
   - **Key**: `VITE_API_URL`
   - **Value**: `https://your-backend.railway.app/api`
   - **Environment**: ✅ Production, ✅ Preview, ✅ Development
4. **احفظ**
5. **أعد Deploy:**
   - Deployments → **⋮** → **Redeploy**

---

### **إذا Frontend على Railway:**

1. **افتح Frontend Service** → **Variables**
2. **أضف متغير جديد:**
   - **Key**: `VITE_API_URL`
   - **Value**: `https://your-backend.railway.app/api`
3. **احفظ** (سيعاد Deploy تلقائياً)

---

### **إذا Frontend على Render:**

1. **افتح Frontend Service** → **Environment**
2. **أضف متغير جديد:**
   - **Key**: `VITE_API_URL`
   - **Value**: `https://your-backend.railway.app/api`
3. **احفظ**
4. **أعد Deploy:**
   - Deployments → **Manual Deploy**

---

## 📝 **ملاحظات مهمة:**

### ⚠️ **`/api` هو قيمة افتراضية فقط!**

في السطر:
```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || '/api'
```

`'/api'` يعمل **فقط في التطوير المحلي** عندما يكون Backend على نفس الخادم.

**في الإنتاج (Production):** يجب أن يكون `VITE_API_URL` رابط كامل مثل:
```
https://crm-backend.railway.app/api
```

---

### ⚠️ **تذكر إضافة `/api` في النهاية**

```
✅ صحيح:  https://crm-backend.railway.app/api
❌ خطأ:   https://crm-backend.railway.app
```

---

### ⚠️ **بعد إضافة المتغير، يجب إعادة Deploy**

**مهم جداً:**  
Vite يُحدث متغيرات البيئة **فقط عند البناء (Build)**.

بعد إضافة `VITE_API_URL`:
1. ✅ احفظ في Environment Variables
2. ✅ أعد Deploy للموقع
3. ✅ انتظر اكتمال Build
4. ✅ جرّب تسجيل الدخول

---

## 🔄 **خطوات التحقق الكاملة:**

### **1️⃣ تحقق من Backend:**

افتح في المتصفح:
```
https://your-backend.railway.app/api/health
```

**إذا رجع `OK`:** ✅ Backend يعمل  
**إذا رجع خطأ:** ❌ مشكلة في Backend

---

### **2️⃣ تحقق من VITE_API_URL:**

افتح Console في الموقع:
```javascript
console.log(import.meta.env.VITE_API_URL)
```

**إذا رجع `undefined`:** ❌ المتغير غير معرف  
**إذا رجع URL صحيح:** ✅ المتغير معرف

---

### **3️⃣ تحقق من Network Requests:**

افتح **Network** tab في Developer Tools:
1. جرّب تسجيل الدخول
2. افحص الطلبات الفاشلة
3. انظر إلى URL المحاول

**إذا كان URL خاطئ:** راجع `VITE_API_URL`  
**إذا كان 401/403:** مشكلة في Authentication  
**إذا كان 500:** مشكلة في Backend

---

## 📋 **قائمة التحقق:**

- [ ] تم التحقق من `VITE_API_URL` في Console
- [ ] تم إضافة `VITE_API_URL` في Environment Variables
- [ ] تم إعادة Deploy بعد إضافة المتغير
- [ ] تم التحقق من Backend (`/health` رجع `OK`)
- [ ] تم تحديث `CORS_ORIGIN` في Backend
- [ ] تم الاختبار مرة أخرى

---

## ✅ **الخلاصة:**

**الكود صحيح 100%** ✅

**المشكلة:** `VITE_API_URL` غير معرف في منصة الاستضافة ❌

**الحل:** أضف `VITE_API_URL` وأعد Deploy ✅

---

**📅 تاريخ:** 2025-02-11  
**🔍 الحالة:** دليل تشخيصي

