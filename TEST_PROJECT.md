# ✅ اختبار المشروع قبل الرفع

## 📋 قائمة التحقق

### ✅ 1. قاعدة البيانات
- [x] تم إنشاء قاعدة البيانات المحلية (SQLite)
- [x] تم تنفيذ Migrations بنجاح
- [x] تم إدخال البيانات الأولية (Seed)

### ✅ 2. Backend
- [ ] Backend يعمل على http://localhost:3000
- [ ] API Docs متاحة على http://localhost:3000/api/docs
- [ ] يمكن تسجيل الدخول بنجاح

### ✅ 3. Frontend
- [ ] Frontend يعمل على http://localhost:5173
- [ ] يمكن الوصول لصفحة تسجيل الدخول
- [ ] لا توجد أخطاء في Console

### ✅ 4. الوظائف الأساسية
- [ ] تسجيل الدخول بـ admin@crm.com / admin123
- [ ] عرض لوحة الإدارة
- [ ] عرض صفحة العملاء
- [ ] عرض صفحة الطلبات
- [ ] عرض صفحة المخزون
- [ ] عرض صفحة الملفات
- [ ] رفع ملف في المخزون
- [ ] رفع ملف في صفحة الملفات
- [ ] إنشاء عميل جديد
- [ ] إنشاء طلب جديد

## 🔧 إذا كان هناك مشاكل:

### Backend لا يعمل:
1. افتح نافذة Backend وابحث عن الأخطاء
2. تأكد من أن Port 3000 غير مستخدم:
   ```powershell
   netstat -ano | findstr :3000
   ```
3. تأكد من تثبيت التبعيات:
   ```bash
   cd backend
   npm install
   ```

### Frontend لا يعمل:
1. تأكد من أن Port 5173 غير مستخدم
2. تأكد من تثبيت التبعيات:
   ```bash
   cd frontend
   npm install
   ```

### قاعدة البيانات:
إذا أردت إعادة إنشاء قاعدة البيانات:
```bash
cd backend
del prisma\dev.db
npx prisma migrate dev --name init
npx prisma db seed
```

## 📝 ملاحظات للرفع:

### للرفع على Render/Production:
1. ✅ استخدم `schema.prod.prisma` (PostgreSQL)
2. ✅ تأكد من `DATABASE_URL` في Environment Variables
3. ✅ تأكد من `JWT_SECRET` و `CORS_ORIGIN`

### للرفع على Supabase + Vercel + Railway:
1. ✅ اتبع دليل `FREE_HOSTING_OPTIONS.md`
2. ✅ استخدم Supabase Database URL
3. ✅ استخدم `schema.prod.prisma` (PostgreSQL)

